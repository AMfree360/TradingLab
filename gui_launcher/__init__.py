"""TradingLab GUI Launcher (thin wrapper).

This package is intentionally UI-only: it orchestrates the existing CLI scripts and
stores reproducible run bundles. It must not duplicate trading logic.
"""
