const fs=require('fs'); const s=fs.readFileSync('gui_launcher/static/js/guided_builder_v2.js','utf8'); const lines=s.split('\n'); let brace=0, paren=0, bracket=0; for(let i=0;i<lines.length;i++){ const ln=lines[i]; for(let ch of ln){ if(ch==='{') brace++; else if(ch==='}') brace--; else if(ch==='(') paren++; else if(ch===')') paren--; else if(ch==='[') bracket++; else if(ch===']') bracket--; } if(i%100===0) console.log('line',i+1,'counts',brace,paren,bracket); } console.log('final counts',brace,paren,bracket);