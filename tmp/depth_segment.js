const fs=require('fs'); const lines=fs.readFileSync('gui_launcher/static/js/guided_builder_v2.js','utf8').split('\n'); let depth=0; for(let i=0;i<lines.length;i++){ const ln=lines[i]; if(i+1>=600 && i+1<=820){ let before=depth; for(const ch of ln){ if(ch==='{' ) depth++; else if(ch==='}') depth--; } let after=depth; console.log((i+1).toString().padStart(4),'before',before,'after',after, ln); } else { for(const ch of ln){ if(ch==='{' ) depth++; else if(ch==='}') depth--; } } } console.log('final',depth);