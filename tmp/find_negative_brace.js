const fs=require('fs'); const s=fs.readFileSync('gui_launcher/static/js/guided_builder_v2.js','utf8'); const lines=s.split('\n'); let brace=0; for(let i=0;i<lines.length;i++){ const ln=lines[i]; for(let ch of ln){ if(ch==='{') brace++; else if(ch==='}') brace--; } if(brace<0){ console.log('Negative brace at line',i+1, ln); process.exit(0);} } console.log('No negative brace, final',brace);